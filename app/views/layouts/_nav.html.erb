<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <div class="container-fluid" >
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <a class="navbar-brand" href="/">RubyGemsProject</a>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if signed_in? %>
        <li class="nav-item">
          <%= link_to '<i class="bi bi-house-door-fill"></i> Home'.html_safe, root_path, class: "nav-link #{'active font-weight-bold' if current_page?(root_path)}" %>
        </li>
          <li class="nav-item">
            <%= link_to '<i class="bi bi-file-richtext-fill"></i> Courses'.html_safe, courses_path, class: "nav-link #{'active font-weight-bold' if current_page?(courses_path)}" %>
          </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "Log in", user_session_path, class: "nav-link #{'active font-weight-bold' if current_page?(user_session_path)}"%>
            </li>
            <li class="nav-item">
              <%= link_to "Sign up", new_user_registration_path, class: "nav-link #{'active font-weight-bold' if current_page?(new_user_registration_path)}" %>
            </li>
        <% end %>
      </ul>
        <% if current_user %>
        <ul class="navbar-nav mb-2 mb-lg-0">
         <li class="nav-item">
         <%= form_tag(courses_path, method: :get) do%>
            <div class="input-group">
              <%= text_field_tag :title, params[:title], autocomplet: 'off', placeholder: 'Search..', class: "form-control" %>
              <button class="btn btn-primary" type="submit" id="button-addon1"><i class="bi bi-search"></i></button>
            </div>
          <% end %>  
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              &nbsp;<i class="bi bi-person-fill"></i>&nbsp;<%= current_user.email %>
            </a>
            <ul class="dropdown-menu">
              <li class="dropdown-item">
                <%= link_to '<i class="bi bi-pencil-square"></i> Edit user'.html_safe, edit_user_registration_path, class: 'btn btn-primary btn-sm' %>
              </li>
              <li class="dropdown-item">
                <%= button_to '<i class="bi bi-power"></i> Log out'.html_safe, destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-sm' %>
              </li>
            </ul>
          </li>
          </ul>
        <% end %>  
    </div>
   </div>
  </div>
</nav>